---
'@querry-kit/nuxt-ui': major
---

Split table controls into internal components, retain `QTable*` as the public Nuxt component names, and replace bundled locale payloads with host-app `querrykit.table.*` keys, nested `texts` overrides, and English fallbacks.
