import { defineConfig } from 'vitepress';

export default defineConfig({
  title: 'Query Kit Nuxt UI',
  description: 'Customizable Nuxt UI table controls',
  themeConfig: { nav: [{ text: 'Guide', link: '/' }] },
});
