# @querry-kit/nuxt-ui

Keep this module framework-neutral beyond its declared Nuxt UI peers. Do not import CRM code, application stores, runtime configuration, or layouts. Public component names must keep the `QuerryKit` prefix and user-visible messages must remain under `querryKit`.
